"""
pollution_indices = [5, 3, 1, 7, 10] # List of numbers
print(min(pollution_indices)) # Prints the element with the minimum value in the list
print(max(pollution_indices)) # Prints the element with the maximum value in the list
cats.sort() # Sort the items in ascending order
print(sum(pollution_indices)) # Sum the elements in the list
print(cats + pollution_indices) # Join two lists
print(pollution_indices.count(10)) # Count the elements that match with the given value
"""
