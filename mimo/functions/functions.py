def say_greeting(name): # Function definition
    return "Hello" + name # Function return
print(f"Message: {say_greeting("Juan")}") # Function calling
# We can access global scope variables inside functions
# We can pass lists as a functions parameters